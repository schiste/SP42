# Suggested Scoring Policies

This directory holds machine-generated or exploratory scoring policies.

Files here must:

- declare `lifecycle: suggested`
- remain reviewable by humans
- never become active without evaluation and explicit approval

This directory is intentionally allowed to contain incomplete or experimental files, but the files should still aim to match the scoring policy schema when possible.
